// Trivial helpers. The eval's "trivial task" prompt asks the agent to reverse a
// string — a one-liner it should just write here, not go skill-shopping for.

export function greet(name) {
  return `Hello, ${name}!`;
}
